package views

import (
	"librevita.org/internal/ui/shared"
	"librevita.org/internal/core/auth"
	"librevita.org/internal/ui/components"
	"librevita.org/internal/ui/layouts"
)

// Profile renders the signed-in user's profile: identity card and the
// color scheme preference (light, system, or dark).
templ Profile(token string, p *auth.Principal, joined string) {
	@layouts.App("Profile", token, p, "profile") {
		<div class="mx-auto max-w-3xl p-4 lg:p-6">
			<section class="rounded-lg border border-gray-200 bg-white p-6 shadow-sm dark:border-gray-700 dark:bg-gray-800">
				<div class="flex items-center gap-4">
					@components.Avatar(shared.Initials(p.Name), "h-16 w-16 bg-indigo-600 text-white text-xl")
					<div class="min-w-0">
						<h2 class="truncate text-lg font-semibold text-gray-900 dark:text-white">{ p.Name }</h2>
						<p class="truncate text-sm text-gray-500 dark:text-gray-400">{ p.Email }</p>
						<p class="mt-1 text-xs text-gray-400 dark:text-gray-400">Joined { joined }</p>
					</div>
					<span class="ml-auto shrink-0">@components.Badge(p.Role.String())</span>
				</div>
			</section>
			<section class="mt-4 rounded-lg border border-gray-200 bg-white p-6 shadow-sm dark:border-gray-700 dark:bg-gray-800">
				@components.CardHeader("Appearance")
				<p class="mt-1 text-sm text-gray-500 dark:text-gray-400">
					Choose how LibreVita looks. System follows your browser or operating system preference.
				</p>
				<div data-lv-theme-pref data-lv-active-class="border-indigo-500 bg-indigo-50 text-indigo-700 dark:border-indigo-400 dark:bg-indigo-900/40 dark:text-indigo-300" class="mt-4">
					<fieldset>
						<legend class="sr-only">Color scheme</legend>
						<div class="grid grid-cols-1 gap-3 sm:grid-cols-3">
							<button type="button" data-lv-theme="light" class="flex items-center gap-3 rounded-lg border border-gray-200 px-4 py-3 text-sm font-medium text-gray-700 hover:bg-gray-50 dark:border-gray-600 dark:text-gray-300 dark:hover:bg-gray-700" aria-pressed="false">
								@themeIcon("light")
								Light
							</button>
							<button type="button" data-lv-theme="system" class="flex items-center gap-3 rounded-lg border border-gray-200 px-4 py-3 text-sm font-medium text-gray-700 hover:bg-gray-50 dark:border-gray-600 dark:text-gray-300 dark:hover:bg-gray-700" aria-pressed="false">
								@themeIcon("system")
								System
							</button>
							<button type="button" data-lv-theme="dark" class="flex items-center gap-3 rounded-lg border border-gray-200 px-4 py-3 text-sm font-medium text-gray-700 hover:bg-gray-50 dark:border-gray-600 dark:text-gray-300 dark:hover:bg-gray-700" aria-pressed="false">
								@themeIcon("dark")
								Dark
							</button>
						</div>
					</fieldset>
				</div>
			</section>
		</div>
	}
}

templ themeIcon(name string) {
	switch name {
	case "light":
		<svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8" aria-hidden="true">
			<path stroke-linecap="round" stroke-linejoin="round" d="M12 3v2.25m6.364.386l-1.591 1.591M21 12h-2.25m-.386 6.364l-1.591-1.591M12 18.75V21m-4.773-4.227l-1.591 1.591M5.25 12H3m4.227-4.773L5.636 5.636M15.75 12a3.75 3.75 0 11-7.5 0 3.75 3.75 0 017.5 0z" />
		</svg>
	case "system":
		<svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8" aria-hidden="true">
			<path stroke-linecap="round" stroke-linejoin="round" d="M9 17.25v1.007a3 3 0 01-.879 2.122L7.5 21h9l-.621-.621A3 3 0 0115 18.257V17.25m6-12V15a2.25 2.25 0 01-2.25 2.25H5.25A2.25 2.25 0 013 15V5.25m18 0A2.25 2.25 0 0018.75 3H5.25A2.25 2.25 0 003 5.25m18 0V12a2.25 2.25 0 01-2.25 2.25H5.25A2.25 2.25 0 013 12V5.25" />
		</svg>
	case "dark":
		<svg class="h-5 w-5" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
			<path d="M17.293 13.293A8 8 0 016.707 2.707a8.001 8.001 0 1010.586 10.586z" />
		</svg>
	}
}
